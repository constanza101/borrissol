---
import { Picture } from 'astro:assets';
import { getLangFromUrl, useTranslations } from '../i18n/utils';
import { waHref } from '../config';
import tuftingInd2 from '../assets/images/tufting-ind2.jpg';
import tallerTufting from '../assets/images/taller-tufting.jpg';
import grupal from '../assets/images/grupal.png';
import toteBag from '../assets/images/tote-bag1.png';
import wristRest from '../assets/images/ws-wrist-rest.jpg';
import grupo2 from '../assets/images/grupo2.jpg';
import { isSummerLabVisible } from '../config/seasonal';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
// Localized paths to the dedicated landings (ca is default = no prefix).
const teamBuildingPath = lang === 'ca' ? '/team-building' : `/${lang}/team-building`;
const borlaPath        = lang === 'ca' ? '/borla' : `/${lang}/borla`;
const pelussetesPath   = lang === 'ca' ? '/pelussetes' : `/${lang}/pelussetes`;
const summerLabPath    = lang === 'ca' ? '/summer-lab' : `/${lang}/summer-lab`;
// Seasonal: the Summer Lab card auto-hides after the course end date.
const summerLabVisible = isSummerLabVisible();
const tuftingRows = [
  {
    name: 'Pelusa',
    size: '50×50 cm',
    price: '78€',
    level:     t('tufting.level.express'),
    duration:  t('tufting.dur.4'),
    drawing:   t('tufting.drawing.basic'),
    finishing: t('tufting.finishing.simple'),
    materials: t('tufting.materials'),
    href:      waHref(t('wa.tufting.pelusa')),
  },
  {
    name: 'Floc',
    size: '50×50 cm',
    price: '108€',
    level:     t('tufting.level.intro'),
    duration:  t('tufting.dur.6'),
    drawing:   t('tufting.drawing.basic'),
    finishing: t('tufting.finishing.improved'),
    materials: t('tufting.materials'),
    href:      waHref(t('wa.tufting.floc')),
  },
  {
    name: 'Vellut',
    size: '70×70 cm',
    price: '158€',
    level:     t('tufting.level.intermediate'),
    duration:  t('tufting.dur.8'),
    drawing:   t('tufting.drawing.choice'),
    finishing: t('tufting.finishing.improved'),
    materials: t('tufting.materials'),
    href:      waHref(t('wa.tufting.vellut')),
  },
  {
    name: 'Pelut',
    size: '80×80 cm',
    price: '181€',
    level:     t('tufting.level.pro'),
    duration:  t('tufting.dur.10'),
    drawing:   t('tufting.drawing.choice'),
    finishing: t('tufting.finishing.improved'),
    materials: t('tufting.materials'),
    href:      waHref(t('wa.tufting.pelut')),
  },
];
---

<section id="workshops" class="workshops" aria-labelledby="workshops-heading">

  <!-- ── Section header ──────────────────────────────────────────────── -->
  <div class="workshops-header">
    <span class="ws-eyebrow reveal">{t('workshops.eyebrow')}</span>
    <h2 id="workshops-heading" class="workshops-headline reveal" data-delay="1">{t('workshops.headline')}</h2>
  </div>

  <!-- ── Block 1: Tufting ────────────────────────────────────────────── -->
  <div class="tufting-block">
    <div class="tufting-inner">
      <div class="tufting-layout">

        <!-- Left: heading + intro + image -->
        <div class="tufting-left reveal">
          <h3 class="tufting-title">Tufting</h3>
          <p class="tufting-intro">{t('tufting.intro')}</p>
          <div class="tufting-img-wrap">
            <Picture
              src={tuftingInd2}
              widths={[480, 768, 1100]}
              sizes="(max-width: 767px) 100vw, 55vw"
              formats={['avif', 'webp']}
              alt="Alumna usando pistola de tufting en taller individual en Borrissol Espai Creatiu, Mataró"
              width={1080}
              height={1547}
              class="tufting-img"
              loading="lazy"
            />
          </div>
        </div>

        <!-- Right: service-selection panel + "what is tufting?" blog link -->
        <div class="tufting-right">
        <div class="tufting-panel reveal" data-delay="1">
          <div class="panel-header">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" style="width: var(--icon-size-inline); height: var(--icon-size-inline); stroke-width: var(--icon-stroke-width); display: inline-block; flex-shrink: 0;" aria-hidden="true"><path d="M12.034 12.681a.498.498 0 0 1 .647-.647l9 3.5a.5.5 0 0 1-.033.943l-3.444 1.068a1 1 0 0 0-.66.66l-1.067 3.443a.5.5 0 0 1-.943.033z"/><path d="M21 11V5a2 2 0 0 0-2-2H5a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h6"/></svg>
            <span class="panel-header-label">{t('tufting.panel.header')}</span>
          </div>
          {tuftingRows.map(row => (
            <div class="tufting-row">
              <div class="tufting-row-info">
                <p class="tufting-row-name">{row.name}</p>
                <p class="tufting-row-meta">
                  {row.level} &middot; {row.duration} &middot; {row.size} &middot; {row.price}
                </p>
                <p class="tufting-row-details">
                  {row.drawing} &middot; {row.finishing} &middot; {row.materials}
                </p>
              </div>
              <a href={row.href} target="_blank" rel="noopener noreferrer" class="btn btn-primary tufting-row-btn">{t('ws.cta')}</a>
            </div>
          ))}
        </div>
          <a href="/blog/que-es-el-tufting-i-com-funciona" class="btn btn-secondary tufting-blog-link reveal" data-delay="2">{t('tufting.blog.cta')}</a>
        </div>

      </div>
    </div>  <!-- /tufting-inner -->
  </div>

  <!-- ── Block 2: Other workshops ────────────────────────────────────── -->
  <div class="other-block">
    <div class="block-inner">

      <h3 class="other-title reveal">{t('ws.other.title')}</h3>

      <div class="other-grid">

        <!-- Group Tufting -->
        <div class="ws-card reveal">
          <div class="ws-card-img">
            <Picture src={tallerTufting} widths={[300, 500, 700]} sizes="(max-width: 767px) 100vw, 25vw" formats={['avif', 'webp']} alt="Taller Grupal de Tufting" width={700} height={494} loading="lazy" />
          </div>
          <div class="ws-card-body">
            <h4 class="ws-name">{t('ws.group.name')}</h4>
            <p class="ws-desc">{t('ws.group.desc')}</p>
            <p class="ws-details">{t('ws.group.details')}</p>
            <div class="ws-card-footer">
              <a href={waHref(t('wa.ws.group'))} target="_blank" rel="noopener noreferrer" class="btn btn-secondary ws-cta">{t('ws.consult')}</a>
              <a href={pelussetesPath} class="link-arrow ws-learnmore">{t('ws.learnmore')}</a>
            </div>
          </div>
        </div>

        <!-- Borla (monthly) -->
        <div class="ws-card reveal" data-delay="1">
          <div class="ws-card-img">
            <Picture src={grupal} widths={[300, 500, 700]} sizes="(max-width: 767px) 100vw, 25vw" formats={['avif', 'webp']} alt="Grupo de alumnas practicando técnica textil en el taller mensual Borla de Borrissol, Mataró" width={700} height={504} loading="lazy" />
            <span class="ws-badge badge badge-light">{t('ws.borla.subtitle')}</span>
          </div>
          <div class="ws-card-body">
            <h4 class="ws-name">{t('ws.borla.name')}</h4>
            <p class="ws-desc">{t('ws.borla.desc')}</p>
            <p class="ws-details">{t('ws.borla.details')}</p>
            <div class="ws-card-footer">
              <a href={waHref(t('wa.ws.borla'))} target="_blank" rel="noopener noreferrer" class="btn btn-secondary ws-cta">{t('ws.consult')}</a>
              <a href={borlaPath} class="link-arrow ws-learnmore">{t('ws.learnmore')}</a>
            </div>
          </div>
        </div>

        <!-- Tote Bag — hidden, will be reactivated later -->
        {false && (
          <div class="ws-card reveal" data-delay="2">
            <div class="ws-card-img">
              <Picture src={toteBag} widths={[300, 500, 700]} sizes="(max-width: 767px) 100vw, 25vw" formats={['avif', 'webp']} alt="Tote Bag Bordada" width={700} height={934} loading="lazy" />
            </div>
            <div class="ws-card-body">
              <h4 class="ws-name">{t('ws.totebag.name')}</h4>
              <p class="ws-desc">{t('ws.totebag.desc')}</p>
              <p class="ws-details">{t('ws.totebag.details')}</p>
              <a href={waHref(t('wa.ws.totebag'))} target="_blank" rel="noopener noreferrer" class="btn btn-secondary ws-cta">{t('ws.consult')}</a>
            </div>
          </div>
        )}

        <!-- Team Building Special -->
        <div class="ws-card reveal" data-delay="2">
          <div class="ws-card-img">
            <Picture src={wristRest} widths={[300, 500, 700]} sizes="(max-width: 767px) 100vw, 25vw" formats={['avif', 'webp']} alt="Reposamuñecas hecho con tufting para el taller Especial Team Building en Borrissol" width={4032} height={3024} loading="lazy" />
          </div>
          <div class="ws-card-body">
            <h4 class="ws-name">{t('ws.teambuilding.name')}</h4>
            <p class="ws-desc">{t('ws.teambuilding.desc')}</p>
            <p class="ws-details">{t('ws.teambuilding.details')}</p>
            <div class="ws-card-footer">
              <a href={waHref(t('wa.ws.teambuilding'))} target="_blank" rel="noopener noreferrer" class="btn btn-secondary ws-cta">{t('ws.consult')}</a>
              <a href={teamBuildingPath} class="link-arrow ws-learnmore">{t('ws.learnmore')}</a>
            </div>
          </div>
        </div>

      </div>

      <!-- Summer Textile Lab — featured, full width, horizontal layout.
           Seasonal: hidden automatically once the course end date has passed. -->
      {summerLabVisible && (
        <div class="summer-card reveal">
          <div class="summer-card-img">
            <Picture src={grupo2} widths={[480, 768, 1000]} sizes="(max-width: 767px) 100vw, 40vw" formats={['avif', 'webp']} alt="Laboratorio Textil de Verano" width={3000} height={4000} loading="lazy" />
          </div>
          <div class="summer-card-body">
            <span class="eyebrow">{t('ws.summer.techniques')}</span>
            <h4 class="ws-name summer-name">{t('ws.summer.name')}</h4>
            <p class="ws-desc">{t('ws.summer.desc')}</p>
            <p class="ws-details">{t('ws.summer.details')}</p>
            <div class="summer-card-footer">
              <a href={waHref(t('wa.ws.summer'))} target="_blank" rel="noopener noreferrer" class="btn btn-primary summer-cta">{t('ws.consult')}</a>
              <a href={summerLabPath} class="link-arrow ws-learnmore">{t('ws.learnmore')}</a>
            </div>
          </div>
        </div>
      )}

    </div>
  </div>

  <!-- ── Footer note ─────────────────────────────────────────────────── -->
  <div class="workshops-note">
    <p class="ws-note-text">{t('workshops.note')}</p>
  </div>

</section>

<style>
  /* ── Section wrapper ───────────────────────────────────────────────── */
  .workshops {
    background: var(--color-white);
  }

  /* ── Section header ────────────────────────────────────────────────── */
  .workshops-header {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-xs);
    text-align: center;
    padding: var(--space-xl) var(--layout-page-padding-h) var(--space-lg);
  }

  .ws-eyebrow {
    font-size: var(--font-size-ui);
    line-height: var(--line-height-ui);
    color: var(--color-muted);
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  .workshops-headline {
    font-size: var(--font-size-display-alt);
    font-weight: var(--font-weight-heading);
    line-height: var(--line-height-heading);
    letter-spacing: var(--letter-spacing-heading);
    color: var(--color-black);
  }

  /* ── Shared block inner ─────────────────────────────────────────────── */
  .block-inner {
    max-width: var(--layout-max-width);
    margin: 0 auto;
    padding: 0 var(--layout-page-padding-h);
  }

  /* ── Block 1: Tufting ──────────────────────────────────────────────── */
  .tufting-block {
    background: var(--color-light);
    padding: var(--space-xl) 0;
    border-top: var(--border-default);
  }

  .tufting-inner {
    padding: 0 var(--space-lg);
  }

  .tufting-layout {
    display: grid;
    grid-template-columns: 2fr 3fr;
    gap: var(--space-xl);
    align-items: start;
  }

  /* Left column */
  .tufting-left {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .tufting-title {
    font-size: var(--font-size-h1);
    font-weight: var(--font-weight-heading);
    line-height: var(--line-height-heading);
    letter-spacing: var(--letter-spacing-heading);
    color: var(--color-black);
  }

  .tufting-intro {
    font-size: var(--font-size-p2);
    font-weight: var(--font-weight-body);
    line-height: var(--line-height-body);
    letter-spacing: var(--letter-spacing-body);
    color: var(--color-muted);
  }

  .tufting-img-wrap {
    width: 100%;
    aspect-ratio: 1 / 1;
    overflow: hidden;
    border-radius: 0;
  }

  .tufting-img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  /* Right panel */
  /* Right column: panel + "what is tufting?" link below it, filling the space
     left when the panel is shorter than the image column. */
  .tufting-right {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .tufting-panel {
    background: var(--color-white);
    border: var(--border-default);
    border-radius: 0;
    box-shadow: var(--shadow-card);
    overflow: hidden;
  }

  .tufting-blog-link {
    align-self: flex-start;
    margin-top: var(--space-md);
    border-radius: var(--radius-sharp);
    font-size: var(--font-size-ui);
  }

  .panel-header {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-xs);
    padding: var(--space-md);
    border-bottom: var(--border-default);
  }

  .panel-header-label {
    font-size: var(--font-size-p2);
    font-weight: var(--font-weight-body);
    line-height: var(--line-height-body);
    color: var(--color-black);
  }

  /* Rows */
  .tufting-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-md);
    padding: var(--space-md);
    border-bottom: var(--border-default);
  }

  .tufting-row:last-child {
    border-bottom: none;
  }

  .tufting-row-info {
    flex: 1;
    min-width: 0;
  }

  .tufting-row-name {
    font-size: var(--font-size-p2);
    font-weight: var(--font-weight-body);
    line-height: var(--line-height-body);
    color: var(--color-black);
  }

  .tufting-row-meta {
    font-size: var(--font-size-p3);
    font-weight: var(--font-weight-body);
    line-height: var(--line-height-body);
    color: var(--color-muted);
    margin-top: 2px;
  }

  .tufting-row-details {
    font-size: var(--font-size-ui);
    font-weight: var(--font-weight-body);
    line-height: var(--line-height-ui);
    color: var(--color-muted);
    margin-top: 4px;
  }

  /* Uses .btn .btn-primary for fill/colour/hover; only the compact, sharp-edged
     variant differences live here. */
  .tufting-row-btn {
    font-size: var(--font-size-ui);
    padding: 0.65em 1.4em;
    border-radius: var(--radius-sharp);
    white-space: nowrap;
    flex-shrink: 0;
  }

  /* ── Block 2: Other workshops ──────────────────────────────────────── */
  .other-block {
    background: var(--color-white);
    padding: var(--space-xl) 0;
    border-top: var(--border-default);
  }

  .other-block .block-inner {
    max-width: none;
    padding: 0 var(--space-lg);
  }

  .other-title {
    font-size: var(--font-size-h3);
    font-weight: var(--font-weight-heading);
    line-height: var(--line-height-heading);
    letter-spacing: var(--letter-spacing-heading);
    color: var(--color-black);
    margin-bottom: var(--space-lg);
  }

  .other-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-md);
    margin-bottom: var(--space-md);
  }

  .ws-card {
    background: var(--color-white);
    border: var(--border-default);
    border-radius: var(--radius-xs);
    overflow: hidden;
    display: flex;
    flex-direction: column;
  }

  .ws-card-img {
    position: relative;
    aspect-ratio: 4 / 3;
    background: var(--color-light);
    flex-shrink: 0;
    overflow: hidden;
  }

  .ws-card-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .ws-card-img .ws-badge {
    position: absolute;
    top: var(--space-xs);
    left: var(--space-xs);
  }

  .ws-card-body {
    padding: var(--space-sm);
    display: flex;
    flex-direction: column;
    flex: 1;
  }

  .ws-name {
    font-size: var(--font-size-h4);
    font-weight: var(--font-weight-heading);
    line-height: var(--line-height-heading);
    letter-spacing: var(--letter-spacing-heading);
    color: var(--color-black);
  }

  .ws-desc {
    font-size: var(--font-size-p2);
    font-weight: var(--font-weight-body);
    line-height: var(--line-height-body);
    letter-spacing: var(--letter-spacing-body);
    color: var(--color-black);
    margin-top: var(--space-xs);
    flex: 1;
  }

  .ws-details {
    font-size: var(--font-size-p3);
    font-weight: var(--font-weight-body);
    line-height: var(--line-height-body);
    color: var(--color-muted);
    margin-top: var(--space-sm);
    margin-bottom: var(--space-md);
  }

  .ws-cta {
    align-self: flex-start;
    margin-top: auto;
    border-radius: var(--radius-sharp);
    font-size: var(--font-size-ui);
  }

  /* Footer row: Consultar button (left) + "Saber más →" link (right).
     Used only on the Team Building card, which links to its dedicated landing. */
  .ws-card-footer {
    margin-top: auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-sm);
  }

  .ws-card-footer .ws-cta {
    margin-top: 0;
  }

  /* Uses .link-arrow (DS) for the styling; only layout deltas here. */
  .ws-learnmore {
    white-space: nowrap;
    padding-right: var(--space-xs);
  }

  /* Featured: Summer Textile Lab — horizontal card */
  .summer-card {
    display: grid;
    grid-template-columns: 2fr 3fr;
    border: var(--border-default);
    border-radius: var(--radius-xs);
    overflow: hidden;
  }

  .summer-card-img {
    background: var(--color-light);
    aspect-ratio: 3 / 2;
    min-height: 300px;
    overflow: hidden;
  }

  .summer-card-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 80%;
    display: block;
  }

  .summer-card-body {
    padding: var(--space-lg);
    display: flex;
    flex-direction: column;
    border-left: var(--border-default);
  }

  .summer-name {
    margin-top: var(--space-xs);
    margin-bottom: var(--space-xs);
  }

  .summer-card-body .ws-desc {
    flex: 1;
  }

  .summer-card-body .ws-details {
    margin-bottom: var(--space-lg);
  }

  .summer-cta {
    align-self: flex-start;
    margin-top: auto;
    border-radius: var(--radius-sharp);
    font-size: var(--font-size-ui);
  }

  /* Footer row: WhatsApp CTA (left) + "Saber más →" link to the landing. */
  .summer-card-footer {
    margin-top: auto;
    display: flex;
    align-items: center;
    gap: var(--space-md);
    flex-wrap: wrap;
  }

  .summer-card-footer .summer-cta {
    margin-top: 0;
  }

  /* ── Footer note ────────────────────────────────────────────────────── */
  .workshops-note {
    text-align: center;
    padding: var(--space-lg) var(--layout-page-padding-h) var(--space-xl);
    border-top: var(--border-default);
  }

  .ws-note-text {
    font-size: var(--font-size-ui);
    line-height: var(--line-height-ui);
    color: var(--color-muted);
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  /* ── Responsive ─────────────────────────────────────────────────────── */
  @media (max-width: 840px) {
    .tufting-layout {
      grid-template-columns: 1fr;
      gap: var(--space-lg);
    }

    .tufting-img-wrap {
      aspect-ratio: 1 / 1;
    }

    .other-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .summer-card {
      grid-template-columns: 1fr;
    }

    .summer-card-img {
      min-height: 220px;
    }

    .summer-card-body {
      border-left: none;
      border-top: var(--border-default);
    }
  }

  @media (max-width: 640px) {
    .other-grid {
      grid-template-columns: 1fr;
    }

    .summer-card-body {
      padding: var(--space-md);
    }
  }
</style>
