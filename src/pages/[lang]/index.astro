---
/**
 * Home route — generates /es, /en, /fr statically from a single source.
 * The default-locale URL / is served by src/pages/index.astro.
 */
import HomePage from '../../layouts/HomePage.astro';
import { nonDefaultLangPaths } from '../../i18n/utils';

export const getStaticPaths = nonDefaultLangPaths;
---

<HomePage />
